<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Sobrescribir vista formulario de métodos de pago para permitir crear, editar y eliminar -->
    <record id="view_account_payment_method_form" model="ir.ui.view">
        <field name="name">account.payment.method</field>
        <field name="model">account.payment.method</field>
        <field name="inherit_id" ref="om_account_accountant.view_account_payment_method_form"/>
        <field name="arch" type="xml">
            <form position="attributes">
                <attribute name="create">1</attribute>
                <attribute name="edit">1</attribute>
                <attribute name="delete">1</attribute>
            </form>
        </field>
    </record>

    <!-- Sobrescribir vista árbol de métodos de pago para permitir crear, editar y eliminar -->
    <record id="view_account_payment_method_tree" model="ir.ui.view">
        <field name="name">account.payment.method</field>
        <field name="model">account.payment.method</field>
        <field name="inherit_id" ref="om_account_accountant.view_account_payment_method_tree"/>
        <field name="arch" type="xml">
            <tree position="attributes">
                <attribute name="create">1</attribute>
                <attribute name="edit">1</attribute>
                <attribute name="delete">1</attribute>
            </tree>
        </field>
    </record>
</odoo>

